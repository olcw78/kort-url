<!DOCTYPE html>
<html lang="en">
  <%- include('components/header') %>
  <body>
    <%- include('components/nav') %>

    <h1 class="text-3xl bg-blue-400">EJS practice</h1>
    <% if (people) { %>
    <h2><%= people.join(', ') %></h2>
    <% } %> <%- include('components/footer') %>
  </body>
</html>
